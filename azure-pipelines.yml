# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      git config --global user.email "t-aksh@microsoft.com"
      git config --global user.name "Akash Sharma"
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'VizUrDataRepoServiceConnection'
    subscriptionId: 'a4a2cee9-4034-4c0c-853b-6f64c2f0a278'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'Intern_Akash_RG'
    location: 'East US'
    templateLocation: 'Linked artifact'
    csmFile: 'CreateWebApp/ADFTutorialARM.json'
    csmParametersFile: 'CreateWebApp/ADFTutorialARM-Parameters.json'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
